cloud:
  aws:
    credentials:
      access-key: key
      secret-key: key
    region:
      static: ca-central-1
    stack:
      auto: false

application:
  bucket:
    name: filehubb


spring:
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2MB
      max-file-size: 5MB
      max-request-size: 10MB

server:
  port: 9090
